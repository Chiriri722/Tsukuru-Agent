# Tsukuru Agent v2.5.0 compatibility update

## Added

- Electron `app.asar`와 NW.js `package.nw`를 원본 보존 staging/provenance 방식으로 처리한다.
- `package.nw` ZIP의 경로 탈출, 절대 경로, 중복 항목, 심볼릭 링크, 파일 수와 해제 크기를 검사한다.
- GDevelop `data.js`를 실행하지 않고 `gdjs.projectData` JSON 객체만 파싱한다.
- 정적 `TextObject::Text.string` 및 `BBText::BBText.text`를 JSON Pointer manifest로 extract, patch, verify, copy-only apply한다.
- `gdjs/`, `libs/gdjs/`, `Extensions/`, `code*.js`를 보호 스크립트로 분류하고 apply 전후 변형을 차단한다.
- loose GDevelop 및 GDevelop-in-`package.nw`의 CLI `extract → patch → verify → apply` 회귀 테스트를 추가했다.
- `Backup` + `Extract` + `.extracteddata`만 보관한 휴대용 RPG 작업 팩을 자동 탐지하고 `Backup` JSON 구조를 검증한다.
- 미디어 추출물이 없는 휴대용 팩 apply가 루트 `System.json` 부재로 실패하던 문제를 수정했다.
- manifest ID 기반 `*_trans.json` 폴더를 `options.translationDirectory` 하나로 안전한 patch/apply에 조립하고, 제외된 미등록·빈 값·동일 값·주석 수를 JSON으로 보고한다.
- 직접 번역된 Extract 때문에 오래된 RPG manifest를 `.extracteddata` 매핑과 현재 텍스트로 재구축하는 `recover` 작업을 추가하고 기존 manifest를 보존한다.
- RPG apply가 추출 전용 `comment_*` marker를 게임 JSON에 재삽입하지 않도록 수정했다.
- Windows launch probe가 NW.js/Electron 자식 프로세스 트리까지 종료하도록 보강했다.
- `Backup`과 현재 RPG 데이터를 함께 검사해 원본부터 존재한 끊어진 참조는 `*_BASELINE` warning으로, 현재 작업본에서 새로 생긴 손상은 blocking error로 분리한다.
- RPG provenance가 있는 Electron ASAR 작업본에서도 `translationDirectory`를 patch→apply→repack→publish 단일 transaction으로 처리하며 실패 시 working extract·원본·기존 출력을 보존한다.
- `recover`에 파일을 쓰지 않는 `dryRun`과 `backup-and-replace`/`fail-if-present` 충돌 정책을 추가했다.
- CLI 배포 ZIP의 UTF-8 entry 순서·timestamp를 고정하고 volatile NTFS extra field/comment를 제거한다. Windows의 대소문자·Unicode·끝 점/공백 별칭 충돌도 거부한다. NW.js `package.nw`도 고정 순서·timestamp·권한으로 조립하며 package verifier가 이 결정을 검사한다.
- 디렉터리형 `package.nw`를 진단하고, `experimentalNwDirectory`를 extract/apply 양쪽에 명시한 경우에만 provenance 기반 별도 게임 복사본 round-trip을 허용한다.
- PE 실행 파일 뒤 appended ZIP을 진단하고, unsigned 단일 후보에 한해 `experimentalNwAppendedZip` opt-in으로 prefix 보존 round-trip을 허용한다. PE certificate table·다중 후보·불일치 layout은 진단 전용이다.
- Electron 내부 GDevelop을 JSON Pointer 기반으로 extract/patch/apply하고, 보호 runtime·ASAR unpacked·외부 resources·원본 archive hash를 재검증한다.
- 비정상 ASAR metadata는 진단 추출하되 재패키징은 `experimentalMalformedAsarRepack` 명시 없이는 거부한다.
- GDevelop `code*.js`는 기본 보호를 유지하고, `experimentalGdevelopCodeStrings` opt-in에서 생성 객체의 직접 `setString`/`setBBText` 정적 리터럴만 Acorn AST로 extract/patch/verify/apply한다. 제외 후보와 파싱 오류는 별도 report에 기록한다.

## Safety behavior

- 일반 RPG 사전 적용도 Extract·매핑·최종 출력을 하나의 rollback 묶음으로 설치한다.
  GUI의 즉시 적용은 검증된 파일을 묶어서 교체하고 실패 시 이전 파일을 복원한다.
- Backup 원문을 기준으로 제어코드·인수·자리표시자·새 빈 값/U+FFFD를 검사한다.
  출력 JSON/YAML/plugin/CSV를 다시 읽어 계획된 위치와 값만 바뀌었는지 확인한다.
- `translationQuality`는 기계적 보존과 언어/문맥 감수를 구분한다. 의미 정렬은
  `not-run`이며 `recover`나 구조 통과로 의미 감수 완료를 표시하지 않는다.
  원문 유지·인명/크레딧·균형 잡힌 따옴표 변경에는 감수 근거를 남긴다.
- 해시 충돌은 전체 수와 최대 100개 상세·생략 수를 반환한다. AppleDouble 파일은
  원본에 보관하면서 데이터 파싱 후보에서 제외한다.

- patch는 같은 파일의 미수정 이웃까지 좌표 누락·범위 오류·중첩을 검사하고,
  동일 파일의 경로 별칭이 별도 매핑 그룹을 만드는 요청을 쓰기 전에 거부한다.
  Windows 장치/확장 네임스페이스 경로도 mutation 전에 거부하며,
  최소 v1 manifest 읽기 호환은 유지한다.
- 원본 archive와 게임 폴더에 직접 pack/apply하지 않는다.
- 컨테이너 작업본에는 절대 원본 경로 대신 archive 상대 경로, SHA-256, 엔진 루트와 파일 목록을 기록한다.
- apply 시 사용자가 명시한 `options.containerSourcePath`를 provenance와 다시 대조한다.
- 원본 변경, 보호 runtime 변경, manifest/JSON Pointer 위조, unsafe ZIP entry, 출력 경로 충돌은 최종 출력 생성 전에 중단한다.
- manifest가 가리키는 Extract·Backup·`.extracteddata` 경로의 심볼릭 링크/정션은 읽기·쓰기 전에 거부한다.

## Known limitations

- GDevelop `code*.js`의 custom extension·hand-written event code·변수·template/연산식·식별자·resource path는 자동 번역하지 않는다. 실험 프로필도 보수적 생성 코드 부분집합만 지원한다.
- `gdjs.projectData`가 strict JSON 객체가 아니거나 난독화·암호화된 내보내기는 진단만 가능하다.
- appended ZIP은 PE32/PE32+와 classic EOCD/central directory의 보수적 부분집합만 지원하며 ZIP64·서명된 실행 파일·다중 후보는 자동 재포장하지 않는다.
- 디렉터리형 `package.nw`는 합성 round-trip과 기존 ZIP 회귀까지 검증했지만 승인된 실제 샘플 launch/playtest가 없어 실험 기능으로 유지한다.
- Electron/GDevelop은 strict `gdjs.projectData` JSON과 정상 ASAR metadata 범위에서 지원한다. 난독화·보호 metadata 우회는 지원하지 않는다.
- 구조 검증과 선택적 launch probe는 실제 플레이테스트를 대체하지 않는다.

## Verification

- TypeScript compile/typecheck 통과
- 깨끗한 `npm ci` 직후 최신 소스의 `npm run verify`: 47 test files, 243 tests, 243 pass, 0 fail; type/version/generated/inventory/supply-chain drift 0
- 고정 seed `npm run test:order`: 243/243 pass
- `npm run benchmark:check`: RPG/Wolf/Tyrano/GDevelop/ASAR/NW.js 6개 case 모두 구조·시간·RSS 상한 통과
- synthetic `package.nw` source SHA-256 불변 및 wrapper copy/repack E2E 통과
- `npm run build:cli`을 두 번 실행해 148,285,977-byte/76-entry `tsukuru-agent-2.5.0-win.zip`이 `fc /b`로 byte-identical임을 확인했다. ZIP SHA-256은 `73658e117b4e8bb479fc15277e0851d900e09e4a283339313ec8474378a4f8c6`이고 내부 app ASAR 3,012 entries, package version/필수 파일/allowlist와 `E_REQUEST_INVALID`/exit1 smoke가 통과했다.
- dirty integration worktree의 비릴리스 검증으로 SHA256SUMS, manifest, SPDX 2.3 SBOM을 두 번 생성해 byte-identical임을 확인했다. manifest는 `sourceTreeDirty: true`를 정직하게 기록하며, 정식 clean-source evidence는 커밋된 깨끗한 트리에서 다시 생성해야 한다.
- Electron 43.4.1 GUI 보안 smoke와 실제 패키징을 재실행했다. portable 109,353,724 bytes, NSIS 109,563,216 bytes이며 packaged GUI는 5초 실행 중 조기 종료 없이 유지되고 자식 3개를 포함해 종료 후 잔류 프로세스가 0개였다.
- `npm audit --omit=dev`와 full audit: 0 vulnerabilities. Electron 23.3.13부터 현재 안정 버전 43.4.1까지와 electron-builder 23.6.0부터 26.15.7까지 모든 rung을 통과했다.
- 실제 RPG 번역 팩: 사전 121파일/21,021항목 중 20,476항목 자동 patch 후 manifest 29,671/29,671 검증 통과
- 오래된 manifest 실제 팩: 9,112항목 재구축(8,940 해시 갱신), apply 결과 7개 JSON이 기존 완료본과 의미상 동일
- 전체 게임 복사본 2개에 번역 data를 오버레이하고 5초 실행 프로브 통과, 종료 후 잔류 프로세스 0개, 원본 세 팩 SHA-256 집계 불변
