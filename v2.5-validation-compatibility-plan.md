# Tsukuru Agent v2.5 계획서: 검증기와 변형 게임 호환성

- 작성일: 2026-08-06
- 기준 버전: Tsukuru Agent v2.0.0
- 목표 버전: v2.5.0
- 저장소: C:\Users\White\Documents\GitHub\Tsukuru Agent\Tsukuru Agent
- 원칙: 에이전트가 JSON으로 호출할 수 있고, 사용자는 터미널에서 결과를 읽을 수 있으며, 원본 게임은 기본적으로 변경하지 않는다.

## 1. 배경과 현재 상태

현재 v2.0은 RPG Maker MV/MZ와 Wolf RPG를 대상으로 verify, extract, patch, apply를 제공한다. TypeScript 검사와 4개 smoke 테스트는 통과했으며, headless Windows zip 배포도 확인되었다.

현재 코드의 한계는 두 곳에 집중된다.

- src/cli/formatDetect.js의 detectFormat는 projectPath와 projectPath/www만 검사하고 data/Data 아래의 JSON 또는 Wolf .mps를 찾는다.
- src/cli/run.js의 opVerify는 추출 디렉터리, manifest, 텍스트 파일, .extracteddata의 존재와 기본 매핑만 확인한다. 재삽입 가능성, 파일 변형량, 시스템 스크립트 손상도, archive 재포장 가능성은 수치화하지 않는다.

따라서 v2.5는 format 문자열을 늘리는 작업이 아니라 다음 4층을 도입하는 작업으로 정의한다.

    container → engine profile → text rules → apply/repack strategy



## 2. 새로 확인된 사례와 조사 결론

### 2.1 사용자 제공 사례와 로컬 샘플

사용자가 제공한 포스트의 다음 절차는 Electron ASAR의 공식 흐름과 일치한다.

    npx asar extract app.asar app_extracted
    수정 및 검증
    npx asar pack app_extracted app.asar

현재 확인된 샘플은 resources/app.asar를 사용하고, 아카이브 안에 project/data, project/js, project/assets, project/effects가 중첩되어 있다. rmmz_core.js 등 MZ 코어, plugins.js, ElectronForMz.js, Live2D/Cubism, Effekseer 흔적이 함께 존재한다.

따라서 이 샘플의 확정 진단은 다음과 같다.

    container: electron-asar
    engine: rpgmz
    wrapper: ElectronForMZ
    features: plugins, Live2D/Cubism, Effekseer, nested-project

댓글의 GDevelop 추정은 가능성 목록으로만 기록한다. 파일 구조만으로 GDevelop을 확정하지 않는다.

### 2.2 웹 조사 결론

- Electron 공식 문서는 resources/app.asar에서 앱을 시작할 수 있고, ASAR는 읽기 전용이며 pack/list/extract 명령을 제공한다고 설명한다.
- Electron ASAR Integrity가 활성화된 빌드는 archive 무결성 불일치 시 종료할 수 있다. 재포장 후 이 메타데이터가 맞는지 검증해야 하며, 우회는 기본 동작으로 삼지 않는다.
- electron-builder는 일반 앱 파일을 app.asar에 넣고 네이티브 모듈이나 실행 파일을 app.asar.unpacked로 분리할 수 있다.
- GDevelop 공식 문서는 수동 데스크톱 내보내기가 Electron 프로젝트를 만들고 electron-builder로 패키징한다고 설명한다. GDevelop은 고정 archive 포맷이 아니라 Electron 내부의 엔진 프로파일로 다룬다.
- ElectronForMZ 공식 저장소는 MZ 코어, Electron, electron-builder, asar: true, project 하위 MZ 프로젝트, ElectronForMz.js 플러그인 구성을 보여준다.
- TyranoScript 공식 문서는 data/scenario/*.ks, data/system/Config.tjs, data/others/plugin을 핵심 계층으로 설명하고, 스크립트 숨김과 동일 계층 패치 기능을 제공한다.
- NW.js 공식 문서는 평문 폴더, package.nw zip, exe 뒤에 붙인 zip을 지원하고 package.json의 main을 앱 진입점으로 사용한다고 설명한다.
- RPG Maker MV 공식 문서는 Windows 배포를 게임 폴더 단위로 설명하므로 www와 NW.js 변형을 별도 후보로 취급해야 한다.



## 3. 목표와 비목표

### 목표

1. 추출 산출물과 수정본을 정량 검증한다.
2. 원문을 다시 적용했을 때 구조적으로 유효한지 dry-run으로 확인한다.
3. 보호 대상 코어/시스템 스크립트의 변형량과 오류를 계산한다.
4. Electron ASAR 안에 중첩된 MV/MZ 프로젝트를 자동 탐지하고 복사본에만 재포장한다.
5. GDevelop, ElectronForMZ, TyranoScript, NW.js 같은 변형을 엔진 프로파일로 분리한다.
6. 에이전트용 JSON은 안정적으로 유지하고, 사람이 읽는 요약은 stderr 터미널에 제공한다.
7. 구버전 v1 manifest와 기존 MV/MZ·Wolf 요청을 계속 읽는다.

### 비목표

- 암호화, DRM, 코드서명, ASAR Integrity를 우회하는 기능
- 임의의 난독화 JavaScript를 안전하게 번역하는 기능
- 원본 게임을 직접 덮어쓰는 기본 동작
- 모든 엔진의 런타임 동작을 자동으로 보장하는 기능
- 사용자 제공 게임 파일을 저장소 fixture로 커밋하는 것



## 4. v2.5 요청·결과 계약

schemaVersion 2를 추가하되 schemaVersion 1 요청은 기존 의미로 처리한다.

요청 예시:

    {
      "schemaVersion": 2,
      "operation": "verify",
      "format": "auto",
      "projectPath": "C:\\Game",
      "outputPath": "C:\\Game_Completed",
      "profile": "standard",
      "options": {
        "verifyDepth": "deep",
        "humanSummary": true,
        "launchProbe": false
      }
    }

기존 result 필드는 유지하고 다음을 추가한다.

    {
      "container": {
        "type": "electron-asar",
        "path": "resources/app.asar",
        "root": "project",
        "confidence": 0.99
      },
      "engine": {
        "type": "rpgmz",
        "wrapper": "ElectronForMZ",
        "features": ["plugins", "live2d", "effekseer"],
        "confidence": 0.99
      },
      "scores": {
        "total": 0,
        "extractionCoverage": 0,
        "mappingIntegrity": 0,
        "reinsertionValidity": 0,
        "protectedScriptIntegrity": 0,
        "containerIntegrity": 0
      },
      "change": {
        "filesChanged": 0,
        "bytesChanged": 0,
        "textBytesChanged": 0,
        "protectedFilesChanged": 0,
        "protectedScriptDamage": 0
      }
    }

stdout는 최종 JSON만 출력한다. humanSummary가 true이면 stderr에 다음을 표시한다.

    [Tsukuru Agent] container=electron-asar engine=rpgmz wrapper=ElectronForMZ
    [Tsukuru Agent] score=96/100 risk=low
    [Tsukuru Agent] extraction=100% reinsert=100% protected-script-damage=0%
    [Tsukuru Agent] original-hash=unchanged output=copy-only



## 5. 검증기 설계

### 5.1 검증 단계

#### A. 입력과 컨테이너 스냅샷

- 원본 프로젝트와 app.asar, app.asar.unpacked, resources 외부 파일의 SHA-256을 기록한다.
- path traversal, 심볼릭 링크, 출력 경로가 원본 내부인지 검사한다.
- Electron package.json, NW.js package.json, MZ/Wolf/Tyrano marker를 읽기 전용으로 수집한다.
- container, engine, wrapper, feature, confidence를 진단 결과에 저장한다.

#### B. 추출 완전성

- 프로파일이 허용한 후보 파일·텍스트 필드 수를 계산한다.
- 실제 manifest entry와 추출 TXT 수를 비교한다.
- JSON, YAML, KS/TJS, UTF-8/Shift-JIS, null-terminated 문자열 등 규칙별 파서를 실행한다.
- 누락, 중복 ID, 빈 매핑, 인코딩 손실, 줄 범위 오류를 분리해서 보고한다.

extractionCoverage는 추출된 유효 텍스트 단위 / 발견된 허용 텍스트 단위 × 100으로 계산한다. 발견하지 못한 영역은 성공으로 간주하지 않고 unknown으로 남긴다.

#### C. 매핑과 데이터 무결성

- manifest entry의 원본 파일, 경로, 해시, 줄 범위, offset, encoding을 검증한다.
- RPG Maker JSON은 파싱, 루트 타입, 키 집합, 배열 길이, 참조 가능한 JSON 구조를 확인한다.
- Wolf는 원본 바이트, offset, 길이 필드, 인코딩, null 종료를 검사한다.
- Tyrano는 KS/TJS 문법 토큰과 닫힘 태그의 균형을 확인한다.
- archive 내부 경로가 정상화되어도 원래 상대 경로가 충돌하지 않는지 확인한다.



#### D. 재삽입 가능성

원본을 직접 수정하지 않고 임시 staging 디렉터리에서 다음을 실행한다.

    extract → 원문 그대로 apply → 구조 검증 → archive pack → 재목록/재파싱

검증 항목은 다음과 같다.

- MV/MZ JSON 재파싱과 engine sentinel 존재
- Wolf 바이너리의 offset, 길이, 원문 바이트 사전검증
- KS/TJS 문법 토큰과 파일 encoding 유지
- ASAR 재목록에서 필수 package.json과 진입점 존재
- app.asar.unpacked와 외부 resources가 보존됨
- 변경하지 않은 파일의 해시 또는 canonical hash가 동일함
- 출력 archive가 빈 파일, 중복 경로, 탈출 경로를 포함하지 않음

번역문 자체의 의미적 정확성은 검증기가 판정하지 않는다. 검증기는 “이 문자열이 엔진이 읽을 수 있는 구조인가”를 판정한다.

#### E. 시스템 스크립트 피해도

보호 집합은 프로파일이 정의한다.

- RPG Maker: rmmz_*.js, main.js, plugins.js, core engine, package.json
- Electron: main/preload/IPC entry, package.json
- Wolf: Data.wolf, .mps 구조 바이트와 header
- Tyrano: tyrano core, system HTML/CSS, plugin runtime
- GDevelop: runtime/lib 파일과 package entry
- NW.js: nw manifest와 launcher metadata

각 보호 파일에 대해 original hash, output hash, parse result, changed bytes, changed ratio를 기록한다. 보호 대상이 변하면 protectedScriptDamage를 0~100으로 계산하고, core 파일 파싱 실패나 integrity failure는 즉시 critical로 승격한다.



### 5.2 점수와 위험 등급

점수는 성공 여부를 대체하지 않는 설명용 지표다.

- extractionCoverage: 20점
- mappingIntegrity: 20점
- reinsertionValidity: 30점
- protectedScriptIntegrity: 20점
- containerIntegrity: 10점

total은 가중 합계이며 critical 오류가 있으면 total과 무관하게 ok=false다.

- 90~100: low, 자동 후속 작업 허용
- 75~89: medium, 경고와 함께 사용자 확인 필요
- 50~74: high, apply 기본 차단
- 0~49: critical, extract 결과를 읽기 전용으로만 보존

changeMagnitude는 별도로 표시한다.

- changedFiles / eligibleFiles
- changedBytes / sourceBytes
- textBytesChanged / extractedTextBytes
- protectedFilesChanged
- protectedScriptDamage
- externalResourceChanged
- archiveHeaderChanged
- integrityMetadataStatus

여기서 “변형량이 크다”와 “손상되었다”를 구분한다. 번역 TXT가 많이 바뀌어도 보호 스크립트와 구조가 유효하면 손상 점수는 증가하지 않는다.




## 6. 호환성 아키텍처

### 6.1 ContainerAdapter

src/core/container에 다음 인터페이스를 추가한다.

- inspect(): 원본 구조, 파일 목록, hash, metadata
- extractTo(stagingDir): 컨테이너를 안전한 임시 폴더로 전개
- packFrom(stagingDir, outputPath): 복사본 archive 생성
- copyUnpacked(outputRoot): app.asar.unpacked와 외부 리소스 보존
- verifyOutput(outputPath): 재목록, 필수 entry, hash 비교

구현 순서:

1. DirectoryContainer: 기존 MV/MZ/Wolf와 nested loose directory
2. AsarContainer: @electron/asar 기반 list/extract/pack
3. ExternalResourceContainer: app.asar.unpacked와 resources 외부 파일
4. NwPackageContainer: package.nw zip 및 manifest
5. UnknownContainer: 감지·보고만 하고 자동 apply하지 않음

asar 처리 규칙:

- 원본 app.asar는 읽기 전용으로 열고 staging에서만 수정한다.
- pack은 반드시 별도 outputPath로 생성한다.
- 원본 app.asar의 hash가 작업 전후 동일한지 확인한다.
- app.asar.unpacked의 native module과 실행 파일은 기본적으로 복사만 한다.
- ASAR Integrity가 탐지되면 metadata 상태를 보고하고, 갱신·서명할 수 없는 경우 apply를 차단한다.
- pack 실패 또는 archive 구조 검증 실패 시 output을 성공으로 보고하지 않는다.


### 6.2 EngineProfile

프로파일은 marker, root 후보, 텍스트 규칙, 보호 경로, 적용 전략을 선언형으로 가진다.

#### rpgmv / rpgmz

- loose data/Data, www/data/Data, nested project/data/Data
- JSON 데이터와 기존 extract/apply 로직 재사용
- js와 plugins.js는 standard에서 보호
- MZ marker는 rmmz_*.js, *.rmmzproject, package.json의 MZ 설명을 조합
- MV/MZ 단독 판정이 불명확하면 rpgmv-compatible로 보고 버전을 단정하지 않음

#### rpgmz-electron / ElectronForMZ

- resources/app.asar 또는 loose resources/app
- project, game, www, app 하위 root 후보를 재귀적으로 확인
- rmmz 코어, MZ 데이터, package.json, ElectronForMz.js를 조합해 confidence를 계산
- local sample과 같은 nested project는 root=project로 해석
- apply는 data와 허용된 텍스트만 수정하고 Electron main/preload/core는 보호

#### gdevelop-electron

- Electron 컨테이너 내부의 HTML/JS/runtime marker, index entry, engine runtime 후보를 검사
- 이름 하나로 확정하지 않고 GDevelop marker와 project metadata가 함께 있을 때만 confidence를 높임
- runtime/lib와 extension code는 기본 보호
- v2.5 standard는 발견·진단 중심이며, 전용 문자열 추출은 full/advanced에서만 허용


#### tyrano / tyranoscript

- data/scenario/*.ks
- data/system/Config.tjs
- data/others/plugin
- scenario text는 line-aware extraction
- plugin runtime과 tyrano core는 보호
- 숨김 또는 자체 archive로 확인되면 unsupported/needs-profile로 보고

#### wolf

- 기존 Data.wolf, Data/data, .mps 규칙을 유지
- copy-only apply와 offset 사전검증을 유지
- nested container가 Wolf data를 포함할 때만 ContainerAdapter 뒤에서 재사용

#### nwjs-webgame

- package.json의 main, index.html, www, package.nw, appended zip 후보
- package.nw는 staging에서 zip-slip 방어를 거쳐 전개
- MZ/Wolf/Tyrano marker가 안에서 발견되면 해당 엔진 프로파일로 재분류
- 엔진이 unknown이면 진단과 파일 목록만 제공하고 자동 patch/apply하지 않음

## 7. 추출·패치·적용 정책

### 프로파일과 범위

- standard: 데이터/시나리오 텍스트만. core, main, preload, plugin runtime은 제외.
- full: 프로파일이 허용한 plugin parameter와 명시적 문자열 영역을 추가하되 파일별 rule과 경고를 남김.
- advanced: 사용자가 include/exclude glob, encoding, protected path override를 명시해야 함.
- 보호 경로 override는 기본값이 아니며 humanSummary와 JSON에 고위험 경고를 남김.


### 적용 파이프라인

    source snapshot
      → staging copy
      → container extract
      → engine extraction/apply
      → structural verify
      → container repack
      → output verify
      → optional launch probe

모든 쓰기는 staging 후 atomic replace로 수행한다. apply 성공 조건은 다음을 모두 만족해야 한다.

- 원본 hash unchanged
- output이 별도 경로
- manifest와 .extracteddata의 매핑 유효
- JSON/바이너리/시나리오 구조 검사 통과
- container listing과 entry point 검사 통과
- 보호 파일 변경 없음
- integrity metadata가 valid 또는 명시적 unsupported 상태가 아님

launchProbe는 기본 false다. true일 때도 실행 파일을 짧은 시간 검사하는 선택 기능으로만 제공하고, 네트워크·입력 자동화·세이브 파일 변경은 하지 않는다.

### 보안 제한

- archive 경로는 output root 밖으로 탈출할 수 없다.
- zip/asar 심볼릭 링크와 junction은 기본 거부한다.
- 파일 수, 전체 압축 해제 크기, 개별 파일 크기에 제한을 둔다.
- manifest의 경로와 profile glob은 정규화 후 다시 검증한다.
- 원본 경로가 outputPath의 조상/자손 관계이면 apply를 거부한다.
- integrity/signature failure를 무시하는 옵션은 제공하지 않는다.


## 8. 구현 단계

### Phase 12: 계약과 컨테이너 기반
- [ ] schemaVersion 2의 container/engine/scores/change/result 필드 추가
- [ ] v1 manifest와 기존 CLI 요청 회귀 테스트 추가
- [ ] ContainerAdapter, DirectoryContainer, AsarContainer 기본 구현
- [ ] @electron/asar runtime dependency와 GPLv3/third-party notice 검토
- [ ] app.asar, app.asar.unpacked, external resources 스냅샷 구현
- [ ] path traversal, symlink, size limit 테스트

### Phase 13: 정량 검증기
- [ ] verify quick/deep 모드 구현
- [ ] extractionCoverage, mappingIntegrity, changeMagnitude 계산
- [ ] JSON 재파싱, Wolf offset, KS/TJS token 검증
- [ ] 원문 재삽입 dry-run과 staging pack 검증
- [ ] protectedScriptDamage와 critical 승격 규칙 구현
- [ ] stderr human summary와 stdout JSON 분리

### Phase 14: 엔진 프로파일과 nested project
- [ ] rpgmz-electron/ElectronForMZ profile 구현
- [ ] nested root 탐색과 confidence 계산
- [ ] GDevelop discovery profile 구현
- [ ] Tyrano/TyranoScript loose profile 구현
- [ ] NW.js package.nw/profile discovery 구현
- [ ] unknown wrapper의 안전한 진단 결과 구현


### Phase 15: 실제 fixture와 회귀
- [ ] 기존 synthetic MV/MZ·Wolf smoke를 그대로 통과
- [ ] synthetic Electron ASAR nested MZ fixture 추가
- [ ] synthetic Tyrano, NW.js, GDevelop marker fixture 추가
- [ ] 실제 샘플은 환경변수 TSUKURU_V25_FIXTURE_ELECTRON_MZ로만 주입
- [ ] 원본 읽기 전용 scan, apply는 임시 복사본에서만 실행
- [ ] archive hash, listing, protected script, external resource 회귀 단언

### Phase 16: 배포·문서
- [ ] README에 verify score와 container/engine 진단 계약 추가
- [ ] CLI JSON 요청 예시와 terminal summary 예시 추가
- [ ] GPLv3 및 @electron/asar 고지 검토
- [ ] typecheck, npm test, headless zip build 검증
- [ ] v2.5 release note와 known limitations 작성

## 9. 테스트 매트릭스와 완료 조건

### 필수 테스트

| 영역 | 성공 조건 |
|---|---|
| 기존 MV/MZ | v1 요청·manifest·extract/patch/apply 결과가 기존과 호환 |
| 기존 Wolf | mps offset, null 종료, copy-only apply 통과 |
| nested MZ | app.asar/project/data와 project/js를 정확히 식별 |
| ASAR round-trip | pack 후 필수 entry, package.json, index와 listing 유효 |
| protected script | rmmz_*, main/preload, plugins.js 무변경 |


| external resources | app.asar.unpacked와 resources 외부 파일 보존 |
| Tyrano | KS/TJS line mapping, tag balance, encoding 경고 |
| NW.js | package.nw 전개와 package.json main 검증 |
| unknown | 자동 apply 차단, 진단 JSON은 생성 |
| safety | original hash unchanged, output conflict, zip-slip 차단 |
| human output | stderr 요약, stdout parseable JSON, exit code 일치 |

### 완료 기준

v2.5는 다음 조건을 모두 만족할 때 완료로 표시한다.

1. v1 요청이 깨지지 않는다.
2. 해당 Electron 샘플을 app.asar → project root → MZ profile로 진단한다.
3. verify 결과에 수치 점수, 원시 변형량, 보호 스크립트 피해도, 경고/오류 원인이 함께 나온다.
4. 원문 재삽입 dry-run과 복사본 재포장이 성공한다.
5. 원본 app.asar와 외부 리소스 hash는 변하지 않는다.
6. MZ 코어·Electron main/preload·플러그인 runtime은 기본 apply에서 변경되지 않는다.
7. GDevelop/Tyrano/NW.js 변형은 확정 가능한 범위만 자동 처리하고, 나머지는 안전하게 unknown/needs-profile로 보고한다.
8. typecheck, npm test, build:cli와 문서 검토가 모두 통과한다.


## 10. 알려진 한계

- 난독화·암호화·자체 바이너리 포맷은 profile 없이는 텍스트 추출이 불가능할 수 있다.
- GDevelop은 Electron 안에 들어간 산출물의 형태가 버전과 export 설정에 따라 달라질 수 있으므로, runtime marker 하나만으로 확정하지 않는다.
- ASAR Integrity나 코드서명은 archive를 다시 만드는 것만으로 해결되지 않을 수 있다. 이 경우 검증기는 실패 원인과 필요한 재서명 단계를 표시한다.
- “게임이 실제로 실행되는가”는 구조 검증과 별개다. launchProbe는 보조 신호이며, 실제 플레이테스트를 대체하지 않는다.

## 11. 조사 출처

- Electron ASAR: https://www.electronjs.org/docs/latest/tutorial/asar-archives
- Electron ASAR Integrity: https://www.electronjs.org/docs/latest/tutorial/asar-integrity
- Electron Application Distribution: https://www.electronjs.org/docs/latest/tutorial/application-distribution
- electron-builder application contents: https://www.electron.build/docs/contents/
- GDevelop desktop Electron export: https://wiki.gdevelop.io/gdevelop5/publishing/windows-macos-linux-with-electron/
- ElectronForMZ: https://github.com/triacontane/ElectronForMz
- electron/asar CLI and API: https://github.com/electron/asar
- TyranoScript project structure: https://tyranoscript.com/usage/tutorial/moji
- TyranoScript distribution packaging: https://tyranoscript.com/usage/studio/export
- TyranoScript patch structure: https://tyranoscript.com/usage/studio/patch
- NW.js package and distribute: https://docs.nwjs.io/For%20Users/Package%20and%20Distribute/
- RPG Maker MV output formats: https://rpgmakerofficial.com/product/MV_Help/page/01_11_04.html

Arca 게시물 URL은 웹 도구에서 직접 열리지 않았으므로, 그 페이지의 기술 결론은 사용자가 붙여넣은 본문과 로컬 샘플 검사에 기반해 보조 자료로만 사용한다.
